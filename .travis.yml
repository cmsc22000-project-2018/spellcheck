language: c

before_install:
    - sudo add-apt-repository -y ppa:snaipewastaken/ppa
    - sudo apt-get update -q
    - sudo apt-get install criterion-dev -y

jobs: 
    include:
        - stage: build
          script: make tests

        - stage: test
          script: ./tests/test-spellcheck